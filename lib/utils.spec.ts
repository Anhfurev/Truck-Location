// Tests can be added here using a testing framework like vitest, jest, etc.
// Currently, this file serves as a placeholder for unit tests.

// Example test structure (when vitest is installed):
// import { describe, it, expect } from "vitest";
// import { cn } from "./utils";
//
// describe("cn function", () => {
//   it("should merge classes correctly", () => {
//     expect(cn("text-red-500", "bg-blue-500")).toBe("text-red-500 bg-blue-500");
//   });
//   it("should handle conditional classes", () => {
//     const isActive = true;
//     expect(cn("base-class", isActive && "active-class")).toBe(
//       "base-class active-class",
//     );
//   });
// });
